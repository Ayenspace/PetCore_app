import 'package:flutter/material.dart';

class VaccinationsScreen extends StatelessWidget {
  const VaccinationsScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Vaccinations')));
}

class AddVaccinationScreen extends StatelessWidget {
  const AddVaccinationScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Add Vaccination')));
}
